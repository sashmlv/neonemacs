;;; yaml-mode --- yaml-mode configuration:
;;; Commentary:
;;; Code:

(setq yaml-indent-offset 2)

;; * Disable error on free variables *
;; Local Variables:
;; byte-compile-warnings: (not free-vars)
;; End:

;;; yaml-mode.el ends here
