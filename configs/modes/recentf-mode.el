;;; recentf-mode --- recentf-mode configuration:
;;; Commentary:
;;; Code:

(recentf-mode 1)
(setq recentf-max-menu-items 50)
(setq recentf-max-saved-items 50)

;; * Disable error on free variables *
;; Local Variables:
;; byte-compile-warnings: (not free-vars)
;; End:

;;; recentf-mode.el ends here
