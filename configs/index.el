;; * Here we will load our config files *
(setq directory-root "/media/user/disk/emacs-config/configs/")
(load (concat directory-root "base"))
;; (load (concat directory-root "first"))
;; (load (concat directory-root "second")
;; (load (concat directory-root "third")
