{
  "name": "template-mcp",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist",
    "nodemon": "nodemon --config nodemon.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "watch": "tsc -b -w tsconfig.json",
    "dev": "npm run build && concurrently -n tsc,srv \"npm run watch\" \"npm run nodemon\""
  },
  "keywords": [],
  "author": "sashmlv",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11",
    "typescript": "^5.9.3"
  }
}
